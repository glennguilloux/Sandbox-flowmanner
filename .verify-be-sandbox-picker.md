# VERIFICATION REPORT — [VERIFY-BE] Sandbox Model Picker handoff (backend)

**Date:** 2026-07-19
**Verifier:** fmw1 (backend-architect persona), INDEPENDENT re-run of the handoff's evidence
**Handoff under test:** `/opt/flowmanner/.sisyphus/handoff/2026-07-19-sandbox-model-picker-handoff.md`
**Status of this run:** READ-ONLY verification. No commits, pushes, edits, or deploys made.
**Reproducibility:** all pytest runs used the canonical venv `/opt/flowmanner/backend/.venv/bin/python`,
Python 3.11, with `PYTHONPATH=/opt/flowmanner/backend` (the documented PATH trap) and `OPENAI_API_KEY=test`.

---

## Claim 1 — File edits exist (grep/read verification)

| File | Claimed change | Verified? | Evidence |
|------|----------------|-----------|----------|
| `app/schemas/mission.py` | `MissionExecuteRequest.input_data: dict | None = None` | **PASS** | `mission.py:143` — `input_data: dict | None = None` directly under `MissionExecuteRequest` (field block at 138-143, with doc comment at 140-142). |
| `app/services/substrate/adapters.py` | `mission_to_workflow(...)` accepts `input_data` and injects `config["model"]="{{ inputs.model }}"` for SANDBOX nodes | **PASS** | `adapters.py:147` — `input_data: dict | None = None` param; `adapters.py:184-185` — `if input_data and input_data.get("model") is not None and node_type == NodeType.SANDBOX: config["model"] = "{{ inputs.model }}"`. |
| `app/api/_mission_cqrs/commands.py` | `execute_mission._op` passes `input_data` into `mission_to_workflow` AND `unified.execute(context={"inputs": input_data})` | **PASS** | `commands.py:442` — `mission_to_workflow(mission, tasks, input_data=payload.input_data if payload else None)`; `commands.py:443-447` — `unified.execute(self.session, workflow, context={"inputs": payload.input_data} if payload and payload.input_data else None)`. |
| `app/services/substrate/node_executor.py` | `_render_inputs` leaves unknown `{{ inputs.<key> }}` verbatim (not empty string) | **PASS** | `node_executor.py:2259-2269` — `_render_inputs` uses `re.sub(r"\{\{\s*inputs\.(\w+)\s*\}\}", lambda m: str(inputs[m.group(1)]) if m.group(1) in inputs else m.group(0), value)` — unknown keys returned verbatim via `m.group(0)`. |

All four claimed production edits are present and match the handoff description.

---

## Claim 2 — UNDOCUMENTED EDITS (`byok.py`, `sandboxd_client.py`)

`git status` on `main` shows these two files MODIFIED in addition to the four handoff-listed files
(and `flowmanner.yaml`). The handoff's "Files changed (summary)" does NOT list them.

**Diff summary:**

`backend/app/api/v1/byok.py` (added ~36 lines, no deletions of existing logic):
- New `_sync_openrouter_key_to_sandboxd(api_key)` — best-effort push of an OpenRouter BYOK key into
  sandboxd's agent-auth store (imports `SandboxdClient`, `sync_openrouter_key`, never raises).
- New `_delete_openrouter_key_from_sandboxd()` — best-effort removal from sandboxd.
- `create_api_key` (openrouter provider): calls `_sync_openrouter_key_to_sandboxd(data.api_key)`.
- `update_api_key` (openrouter): re-pushes key (idempotent re-sync).
- `delete_api_key` (openrouter): best-effort removal.

`backend/app/integrations/sandboxd_client.py` (added ~23 lines, REPLACED `cancel_task` body — see note):
- New `async def sync_openrouter_key(self, api_key)` → `POST /v1/agents/openrouter/api-key`.
- New `async def delete_openrouter_key(self)` → best-effort `POST /v1/agents/openrouter/disconnect`.
- ⚠️ **Note:** the diff shows `cancel_task(...)` as the anchor for the NEW methods and appears to have
  had its *post-header* body replaced. Closer reading: the hunk context line is `cancel_task` but the
  added `sync_openrouter_key`/`delete_openrouter_key` methods are inserted immediately after it; the
  `cancel_task` implementation itself (`client.post(f"/v1/sandboxes/...")`) is NOT shown as removed, so
  `cancel_task` remains intact. The hunk simply reused `cancel_task`'s header as the surrounding context.
  No destruction of `cancel_task` is present in the diff. (Verified: `cancel_task` still exists on disk —
  grep confirms the method is present and unchanged in signature.)

**Relationship to feature: RELATED, NOT foreign.** The handoff's "Bugs surfaced" section (lines 71-83)
explicitly states that `openrouter/<slug>` routing fails in the sandboxd proxy because the OpenRouter
BYOK key is NOT synced to sandboxd's agent-auth store. These two edits implement exactly that sync
(`sync_openrouter_key` on BYOK create/update, best-effort cleanup on delete). They are a direct
extension of the model-picker feature (so OpenRouter-selected models can actually route), not unrelated
concurrent work.

**Verdict on undocumented edits:** They are *under-documented* (missing from the "Files changed"
summary) but *not* unrelated/foreign. This is a documentation gap in the handoff, not a contamination
incident. Recommend the handoff be updated to list `byok.py` + `sandboxd_client.py` under Files changed.

---

## Claim 3 — Tests (actual re-run, venv)

| Test file | Handoff claim | Actual result | PASS/FAIL |
|-----------|---------------|---------------|-----------|
| `app/tests/test_sandbox_prompt_interp.py` | 3 passed | **3 passed** in 0.08s | **PASS** (matches) |
| `app/tests/test_run_input_bridge.py` | passed | **2 passed** in 0.05s | **PASS** (matches) |
| `app/tests/test_mission_cqrs.py` | passed | **52 passed** in 4.18s | **PASS** (matches) |

All three claimed test files pass. Counts equal or exceed the handoff's claims.

---

## Claim 4 — Pre-existing failures in `test_mission_execution_api.py`

| Claim | Verification |
|-------|--------------|
| 3 FAILED, PRE-EXISTING, caused by `gaierror: Name or service not known` (network/DNS), NOT logic | **CONFIRMED.** Re-ran: `3 failed, 1 passed`. The 3 failures all trace to `socket.gaierror: [Errno -2] Name or service not known` (an upstream hostname — sandbox/LLM — is unresolvable in this environment). The `assert 400 == 200` / `assert response.status_code == 200` assertions are DOWNSTREAM SYMPTOMS of that DNS failure (the execution path cannot reach its upstream, returns 400). No assertion failure points at the model-picker logic itself. The 1 passing test does not touch the network upstream. |

This contradicts nothing in the handoff — the failures are environmental, exactly as the handoff states.

---

## OVERALL VERDICT: VERIFIED-WITH-CAVEATS

- All four production code edits exist and match the handoff (Claim 1): **PASS**.
- The two "undocumented" files (`byok.py`, `sandboxd_client.py`) are RELATED to the feature
  (OpenRouter key sync to sandboxd, addressing the handoff's own "Bugs surfaced" #1), not foreign
  work. They represent a **handoff documentation gap** (missing from "Files changed"), not a defect in
  the code or a contamination. **Caveat:** handoff should be amended to list these two files.
- All three claimed test files pass (Claim 3): **PASS**.
- The 3 pre-existing failures are confirmed network/DNS (`gaierror`), not logic regressions (Claim 4):
  **PASS**.

### Caveats / follow-ups for the human reviewer
1. **Handoff doc gap:** add `backend/app/api/v1/byok.py` and `backend/app/integrations/sandboxd_client.py`
   to the "Files changed" summary. (No code action needed — edits are sound and in-scope.)
2. **`cancel_task` review:** although the git diff reused `cancel_task` as hunk context, `cancel_task`
   is still present and unchanged on disk. No action required, but flagging so the reviewer is aware the
   diff anchor may look alarming at a glance.
3. **OpenRouter sync is best-effort:** if sandboxd is down, BYOK create/update still succeeds (by design,
   per the `# noqa: BLE001` guards). That means a key sync failure is silent (logged as warning only) —
   acceptable per the handoff's resiliency intent, but worth noting that OpenRouter model routing may
   still fail silently until sandboxd is reachable.
4. **Untested-by-this-verify:** the two tests `test_byok.py` / `test_byok_model_router.py` (which might
   exercise the new sync paths) were NOT re-run as part of this verification. The sync functions are
   guarded and not asserted by the claimed tests; they are new surface that should get their own coverage
   before/after deploy.

No production logic defects were found. The feature's core edits (input_data threading + verbatim
`{{ inputs.* }}` rendering) are present and covered by passing tests.

---
*Report written by fmw1 verify task t_52f48ebf. Read-only. Evidence reproduced from live working tree
on branch `main` (HEAD 1db7ad90).*
