1. Search for stub classes (e.g. _StubTrace, _StubSpan patterns)
2. Abstract methods with `pass` — base class methods never overridden
3. `pass` in error handlers — bare `except: pass`
4. Placeholder metrics/data — functions documented as "placeholder" or returning hardcoded numbers
5. TODO comments about loading from DB, persisting, implementing real logic
6. Placeholder credentials/keys — hardcoded "placeholder" API keys
7. Stub services — whole services documented as stubs
8. Methods with "not yet implemented" or NotImplementedError patterns
9. `pass` in async function bodies — stub methods that do nothing