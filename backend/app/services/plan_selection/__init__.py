"""Cost-aware plan selection — K-Plan Scored Pick.

Generates multiple plan candidates, scores them deterministically,
and selects a winner by configurable policy (min_cost, max_quality,
balanced, auto).
"""
