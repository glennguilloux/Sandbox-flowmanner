# VER — Verification of W1–W4 Subsystem Analyses (2026-07-10)

**Date:** 2026-07-10
**Worker:** roadmap deep-analysis (claimed `t_011fa203` after W1–W4 complete)
**Method:** re-grep every claim against `backend/app` live code; cross-check the 5 VER criteria from the task body. Also assessed whether the *prior* (2026-07-07) W1–W4 docs are trustworthy.

---

## 1. The 5 VER criteria — result

| # | Criterion | Result | Evidence |
|---|-----------|--------|----------|
| 1 | No claim the hardcoded allowlist still exists | ✅ PASS | All VERIFIED reports describe it as **replaced** by 3-gate. No report claims it still exists. (The word "hardcoded" appears only in correction tables.) |
| 2 | No suggestion to flip untagged default → opt_in | ✅ PASS | Every VERIFIED report states fail-closed is correct; flipping is REJECTED (security incident). Stale `W1-tools-allowlist.md` also rejects it. |
| 3 | Dual-write flagged as BLOCKED / doc missing | ⚠️ **CRITERION ITSELF STALE** | The task body expects "dual-write correctly flagged as BLOCKED (doc missing)." Reality: `docs/DUAL-WRITE-DECISION.md` **exists** + decision **EXECUTED** (2026-07-07). VERIFIED reports correctly say EXECUTED. The criterion should be updated to "dual-write flagged as EXECUTED." |
| 4 | Marketplace described as seed-only | ✅ PASS | W4 VERIFIED §3: `_SEED_LISTINGS`, `seed_marketplace_listings`, no user-gen path. |
| 5 | Chat plumbing wins (fresh_session/BackgroundTaskManager) acknowledged | ✅ PASS | W2 VERIFIED §4: both verified real + rationale given. |

**Overall: 4/5 PASS; criterion #3 is obsolete (the premise it checks is false).**

---

## 2. CRITICAL: the prior (2026-07-07) W1–W4 docs are UNTRUSTWORTHY

VER discovered the older analysis docs contain **stale or wrong claims** that contradict live code. They must be **superseded**, not cited:

| Prior doc | Wrong claim | Live reality (VERIFIED) |
|-----------|-------------|--------------------------|
| `W1-tools-allowlist.md` | lists a specific 11-tool `opt_in` set | Current `opt_in` = a **different** 20 tools; the brief's 11 are now untagged→hidden |
| `W1-tools-allowlist.md:151` | "dual-write BLOCKED" | EXECUTED (doc exists) |
| `W3-missions-swarm.md:86` | "`consensus_strategy`/`consensus_config` are write-only. Documented feature does not exist" | **FALSE** — `swarm_protocol.py` has debate/handoff/escalation protocols; `swarm_consensus_rounds.strategy_used` records it |
| `W2-chat-sse.md` | "SSE keepalive missing (Phase 1.3)" | **FALSE** — implemented `chat_service.py:201-209` |
| `W4-memory-rag-marketplace.md` | (implies relevance on chat) | `relevance_score` is web-search only, not chat |

**Action:** the `*-VERIFIED-2026-07-10.md` set is authoritative. The 2026-07-07 W1–W4 docs should be archived or deleted to prevent the stale claims propagating into roadmap slices. The branch task `t_1b8e2af5` (synthesize) must consume the VERIFIED set, not the originals.

---

## 3. New gaps surfaced during VER (not in original task criteria)

- **G1 — Memory privacy audit: VERIFIED WIRED (self-corrected 2026-07-10).** Initial VER pass misread `_safe_audit` as a no-op. Actually `self.audit = audit or _MemoryCorrectionAudit()` (`personal_memory_service.py:431`); `_safe_audit` dispatches to `_MemoryCorrectionAudit._emit()` → `memory_correction_events` via `MemoryCorrectionService`. All default call sites use it. **No gap.** See W4 §1b (Epic 2.1).
- **G2 — Misleading code comment.** `chat_service.py:1473-1475` comment says untagged default is `opt_in` (wrong; behavior is `hidden`). A future maintainer could "fix" it to opt_in = security incident. Patch the comment.
- **G3 — DB vs in-memory visibility default mismatch.** `Tool.visibility` DB default may be `"public"` while in-memory `ToolMetadata` defaults `"hidden"`. Reconcile to avoid a hydration surprise.
- **G4 — Gate-3 "no scopes = unrestricted."** New `opt_in` tools being tagged should get conscious `required_scopes` assignment, not left unscoped.

**Epic 2.1 note:** `docs/EPIC-2.1-CANONICAL-STORE-DESIGN.md` is a design doc that has been **built** (commit `84403041`). Reviewer writes now land in `personal_memory_claims` via `create_from_proposal`; `MemoryIntegration` deleted. The doc's "no code, no migration" header is obsolete — mark implemented.

---

## 4. Verification method note

Every count in the VERIFIED reports was produced by `grep`/`find` against `backend/app` on 2026-07-10 — not transcribed from the brief or the prior docs. The verifier guard fired repeatedly: 4 brief claims + ~6 prior-doc claims were caught as stale.

---

## 5. Conclusion

W1–W4 VERIFIED analyses are **grounded and trustworthy**. The original 5-criteria checklist mostly passes; criterion #3 is itself obsolete (dual-write is done, not blocked). The older W1–W4 docs are stale and should be superseded by the VERIFIED set before any Phase 2+ slicing consumes them.

---

*Generated by roadmap deep-analysis worker. Consume the `*-VERIFIED-2026-07-10.md` files, not the 2026-07-07 originals.*
