# VERIFY-FE: Sandbox Model Picker handoff — frontend tsc + wiring

**Verifier**: fmw2 (independent read-only verification)
**Date**: 2026-07-19
**Handoff under test**: `/opt/flowmanner/.sisyphus/handoff/2026-07-19-sandbox-model-picker-handoff.md`
**Read root**: `/home/glenn/f` → `/home/glenn/FlowmannerV2-frontend` (master branch)
**tsc target**: `/home/glenn/f` (master)

## IMPORTANT CAVEAT on the worktree

The task spawned a worktree at `/home/glenn/FlowmannerV2-frontend/.worktrees/t_d316ea57`
(branch `agent/2026-07-19-verify/sandbox-picker-fe`). That worktree does **NOT** contain
the feature files — `src/lib/useSandboxModels.ts` does not exist there, and neither
`page-client.tsx` references the hook. The feature under test is committed on the
`main` branch at `/home/glenn/f` (HEAD `a4e3ae0a`).

The task's hard rule says "Use `/home/glenn/f` for all reads," and claim 4 instructs to run
tsc and, if the worktree lacks node_modules, symlink it. Since the worktree tree lacks the
feature entirely, a tsc run there would NOT exercise this handoff. I therefore ran tsc
against `/home/glenn/f` (master), which is where the actual feature lives. This is the
only meaningful place to verify the handoff. Recorded as a caveat, not a failure of the
feature itself.

## Per-claim results

| # | Claim | Result | Evidence |
|---|-------|--------|----------|
| 1 | `src/lib/useSandboxModels.ts` exists, exports `useSandboxModels()` that fetches `Byok.listAvailableModelsApiByokModelsGet()` (GET /api/v1/byok/models), merges static glm-5 + glm-5.2, returns `{id,name,provider}[]` | **PASS** | File present at `/home/glenn/f/src/lib/useSandboxModels.ts` (87 lines). `export function useSandboxModels()` at line 56. Line 65 calls `Byok.listAvailableModelsApiByokModelsGet()`. `FREE_TIER_MODELS` (lines 26-29) = glm-5 + glm-5.2. Returns `SandboxModel[]` with `{id,name,provider}` (interface lines 19-23). |
| 2 | Mission dashboard wiring | **PASS** | `/home/glenn/f/src/app/[locale]/mission-dashboard/page-client.tsx`: imports `ModelSelector` (line 28) + `useSandboxModels` (line 29); `<ModelSelector>` rendered in Quick Actions (lines 507-512); `handleExecute` (lines 200-211) builds `const body = selectedModel ? { input_data: { model: selectedModel } } : undefined;` and calls `Missions.executeMissionApiMissionsMissionIdExecutePost(missionId, "v2", body as any)` (line 204). |
| 3 | Blueprint wiring | **PASS** | `/home/glenn/f/src/app/[locale]/(dashboard)/blueprints/page-client.tsx`: imports `ModelSelector` (line 45) + `useSandboxModels` (line 46); `<ModelSelector>` rendered per blueprint row (lines 781-786); `handleExecute` (lines 507-519) calls `startRun(id, selectedModel ? { model: selectedModel } : undefined)` (line 510). `startRun` signature (`src/lib/api/runs.ts:129`) is `(blueprintId, inputData?, ...)` so `{ model }` is a valid `Record<string, unknown>`. |
| 4 | TypeScript compiles (`tsc --noEmit` exit 0, clean) | **PASS** | Ran `cd /home/glenn/f && ./node_modules/.bin/tsc --noEmit`. Exit code **0**, zero output (no errors). |

## TypeScript compile — actual output

```
$ cd /home/glenn/f && ./node_modules/.bin/tsc --noEmit
(empty stdout / stderr)
$ echo $?  -> 0
```

## Overall VERDICT

**VERIFIED-WITH-CAVEATS**

All four handoff claims are independently confirmed against the actual source:
- The hook exists with the exact described behavior (BYOK fetch + glm-5/glm-5.2 merge, `{id,name,provider}` shape).
- Both pages import the hook + `ModelSelector`, render the selector, and pass the model through the documented execute paths (`executeMissionApiMissionsMissionIdExecutePost(missionId,"v2",{input_data:{model}})` and `startRun(id,{model})`).
- `tsc --noEmit` is clean (exit 0).

CAVEAT: The feature is committed on `master` (`/home/glenn/f`), not in the spawned
verification worktree (`t_d316ea57` / branch `agent/2026-07-19-verify/sandbox-picker-fe`),
which does not contain these files at all. The verification was performed against
`/home/glenn/f` per the task's "use `/home/glenn/f` for all reads" rule. The handoff's
self-report of a clean tsc is accurate, but the feature was never merged into the verify
branch — anyone re-running tsc inside the worktree would find the feature absent, not failing.
